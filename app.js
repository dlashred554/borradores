const connect=document.querySelector('#connect');const status=document.querySelector('#status');const video=document.querySelector('#video');const send=document.querySelector('#send');connect.onclick=()=>{window.location.href='/auth/login'};video.onchange=()=>{send.disabled=!video.files.length};fetch('/auth/status').then(r=>r.ok?r.json():null).then(d=>{if(d&&d.connected){document.body.classList.add('connected');connect.textContent='TikTok conectado';status.textContent='Cuenta conectada.'}}).catch(()=>{});send.onclick=()=>{status.textContent='Preparando subida...';};